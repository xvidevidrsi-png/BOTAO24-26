build:
  pythonVersion: 3.10

services:
  - type: worker
    name: discord-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"

  - type: web
    name: bot-webpage
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web.py"
